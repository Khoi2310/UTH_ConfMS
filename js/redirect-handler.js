// ================= REDIRECT HANDLER =================
// Ensure we're using hash-based routing to avoid server 404 errors
(function() {
    // This project uses hash-based routing, so direct deep-links are generally safe.
    // Keep this file as a no-op placeholder for future server-side routing needs.
})();